<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ses_mail_tracking_view_mail_tracking_email_form" model="ir.ui.view">
        <field name="model">mail.tracking.email</field>
        <field name="inherit_id" ref="mail_tracking.view_mail_tracking_email_form"/>
        <field name="priority" eval="20"/>
        <field name="arch" type="xml">
            <field name="state" position="after">
                <field name="ses_state" widget="statusbar"/>
            </field>                           
        </field>
    </record>

</odoo>
